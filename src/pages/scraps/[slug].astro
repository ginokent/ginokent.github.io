---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { formatDateTime, translations } from '../../lib/i18n';
import { getScrapSeoTitle } from '../../lib/scrap';

export async function getStaticPaths() {
  // 日本語記事のみ（en/ で始まらない）
  const scraps = await getCollection('scraps', ({ slug, data }) => !slug.startsWith('en/') && !data.draft);
  return scraps.map((scrap) => ({
    params: { slug: scrap.slug },
    props: { scrap },
  }));
}

const { scrap } = Astro.props;
const { Content } = await scrap.render();
const t = translations.ja;
---

<BaseLayout
  title={getScrapSeoTitle(scrap.body)}
  ogType="article"
  publishedAt={scrap.data.publishedAt}
  lang="ja"
  canonicalPath={`/scraps/${scrap.slug}`}
>
  <a href="/scraps" class="back-link">&larr; {t.scraps}</a>
  <article class="scrap-article">
    <header class="scrap-article-header">
      <div class="scrap-article-meta">
        <time datetime={scrap.data.publishedAt.toISOString()}>
          {formatDateTime(scrap.data.publishedAt, 'ja')}
        </time>
      </div>
    </header>

    <div class="scrap-content">
      <Content />
    </div>

    {scrap.data.tags.length > 0 && (
      <footer class="scrap-article-tags">
        {scrap.data.tags.map((tag) => (
          <span class="scrap-tag">{tag}</span>
        ))}
      </footer>
    )}
  </article>
</BaseLayout>

<style>
  .back-link {
    display: inline-block;
    color: var(--text-secondary);
    text-decoration: none;
    font-size: 0.875rem;
    margin-bottom: 1rem;
  }

  .back-link:hover {
    color: var(--accent);
  }

  .scrap-article {
    max-width: 42rem;
    margin: 0 auto;
    background: var(--bg-secondary);
    border-radius: 0.5rem;
    padding: 1.5rem;
    border: 1px solid var(--border);
  }

  .scrap-article-header {
    margin-bottom: 1rem;
  }

  .scrap-article-meta {
    font-size: 0.875rem;
    color: var(--text-secondary);
  }

  .scrap-article-tags {
    margin-top: 1rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .scrap-tag {
    font-size: 0.75rem;
    color: var(--text-secondary);
    background: var(--bg);
    padding: 0.25rem 0.5rem;
    border-radius: 0.25rem;
  }

  .scrap-content {
    font-size: 0.9375rem;
    line-height: 1.7;
    overflow-wrap: break-word;
    word-break: break-word;
  }
</style>
